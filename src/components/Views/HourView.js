import React from "react";
// Components

const HourView = ({ weather }) => {
    return (
        <div className="hour-view">
            <h1>Hourly</h1>
        </div>
    );
}

export default HourView;