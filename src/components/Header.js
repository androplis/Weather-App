import React from 'react';

// Components
import SearchForm from './SearchForm.js';

const Header = () => {
    return (
        <header>
            <h1>LOGO</h1>
            <SearchForm />
        </header>
    );
}

export default Header;